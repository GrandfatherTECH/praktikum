Reference sample documents for Phase 4 are stored in `backend/app/templates/samples/`.

Current generation in this phase is programmatic via `python-docx`, using those samples as layout and field references:
- `order-sample.docx`
- `order-extract-sample.docx`
- `phase4-notes.docx`

Real production templates can replace these references later without changing the deployment architecture.
